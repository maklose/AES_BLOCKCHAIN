pragma solidity >=0.4.25 <0.6.0;

contract HendrikTest {
    // Funktion checkt ob die Arbeitsstunden erreicht sind
    // und spielt mit Counter zusammen
    function Arbeitsstundentest (){
        if (balance = 400 )
        return;
        sent(address, balance, Empfänger)
    }

    function 
    }